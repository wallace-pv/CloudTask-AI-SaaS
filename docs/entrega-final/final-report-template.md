# Relatório final — CloudTask AI SaaS

> **Template de entrega** (Aula 12). Copie este arquivo, preencha as seções e
> entregue conforme a orientação da disciplina. Substitua os `<...>`.

---

## 1. Identificação

- **Aluno(a):** `<nome>`
- **RU / matrícula:** `<...>`
- **Disciplina:** Computação em Nuvem — UNINTER
- **Repositório:** `<link do seu fork/branch>`
- **Data:** `<dd/mm/aaaa>`

## 2. Resumo do projeto

`<2–4 linhas: o que é o CloudTask, o que ele faz, qual stack usa.>`

## 3. O que foi implementado (por semana)

| Semana | Entreguei | Evidência (print / comando / endpoint) |
| --- | --- | --- |
| 1 — FastAPI + Docker | `<...>` | `<...>` |
| 2 — PostgreSQL + config | `<...>` | `<...>` |
| 3 — S3 + Kind | `<...>` | `<...>` |
| 4 — ECR + EKS | `<...>` | `<...>` |
| 5 — HPA + DynamoDB | `<...>` | `<...>` |
| 6 — CDK + entrega | `<...>` | `<...>` |

## 4. Arquitetura

`<Cole/descreva a arquitetura. Pode reaproveitar o diagrama de
final-architecture.md e adaptar ao que VOCÊ subiu.>`

## 5. Como executar (reprodutível)

```bash
# passos mínimos para subir o seu projeto
<...>
```

## 6. Decisões e trade-offs

`<Ex.: usei Postgres como Pod em vez de RDS porque...; deixei nat_gateways=0
para não gastar...; fallback local porque...>`

## 7. Custos

- Recursos que cobraram: `<EKS, EC2, ...>`
- Estimativa do período: `<US$ ...>`
- Confirmação de limpeza: `<sweep do deployment-checklist anexado>`

## 8. LGPD e segurança

- `<resumo do lgpd-checklist preenchido; o que foi feito e o que ficaria
  pendente em produção real>`

## 9. Dificuldades e aprendizados

`<O que travou, como resolveu, o que aprendeu de mais importante.>`

## 10. Anexos

- [ ] `lgpd-checklist.md` preenchido
- [ ] `deployment-checklist.md` (sweep de limpeza) preenchido
- [ ] Prints / logs das evidências da seção 3
