# Dependências adicionais para rodar testes (target `test`, usado no CI).
# NÃO entra na imagem de produção.
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0
coverage>=7.4,<8.0
